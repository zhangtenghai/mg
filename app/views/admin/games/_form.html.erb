<%= form_for([:admin,@game], :html => { :class => "form-horizontal" }) do |f| %>
  <div class="form-body">
    <%= f.error_messages %>
    <h4 class="form-section">基本信息</h4>
    <div class="form-group">
      <%= render "shared/column", name: "名称", content: f.c_text_field(:name) %>
      <%= render "shared/column", name: "原名", content: f.c_text_field(:origin_name) %>
      <%= render "shared/column", name: "发售日", content: f.c_datetime_field(:sale_date) %>
    </div>
    <div class="form-group">
      <%= render "shared/column", name: "开发商", content: f.c_text_field(:developer) %>
      <%= render "shared/column", name: "发行商", content: f.c_text_field(:publisher) %>
      <%= render "shared/column", name: "官网", content: f.c_text_field(:website) %>
    </div>
    <div class="form-group">
      <%= render "shared/column", name: "评级", content: f.c_text_field(:rating) %>
      <%= render "shared/column", name: "售价", content: f.c_text_field(:sale_price) %>
      <%= render "shared/column", name: "评分", content: f.c_text_field(:score) %>
    </div>
    <div class="form-group">
      <%= render "shared/column_file", name: "背景图(1600x648)", content: f.file_field(:head_img), url: @game.head_img.url %>
      <%= render "shared/column_file", name: "图像(160x160)", content: f.file_field(:avatar), url: @game.avatar.url %>
      <%= render "shared/column_file", name: "列表图(314x202)", content: f.file_field(:list_img), url: @game.list_img.url %>
    </div>
    <div class="form-group">
      <label class="control-label col-md-1">标签</label>
      <div class="col-md-11">
        <%=f.c_text_field(:tag_list) %>
      </div>
    </div>
     <div class="form-group">
      <label class="control-label col-md-1">简介</label>
      <div class="col-md-11">
        <%= f.text_area(:summary, :class => "form-control") %>
      </div>
    </div>
  </div>

  <div class="form-actions right">
    <button type="submit" class="btn">保存</button>
  </div>
<% end %>

<% content_for :page_scripts do %>
<%= javascript_include_tag "games" %> 
<% end %>